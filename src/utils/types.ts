export type ShopItemType = {
    id: string;
    name: string;
    cost: number;
  };

export type AdType = {
    adId: string;
    message: string;
    reward: number;
    expiresIn: number;
    probability: string;
  };
